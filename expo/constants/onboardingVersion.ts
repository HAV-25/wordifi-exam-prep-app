/**
 * Onboarding version switch.
 *
 * 'prelaunch' → legacy placeholder flow (onboarding_prelaunch)
 * 'launch'    → new Stitch-designed flow (onboarding_launch) v1.0
 *
 * Change this single value to flip which flow the app uses.
 */
export const ONBOARDING_VERSION: 'prelaunch' | 'launch' = 'launch';

export const ONBOARDING_LAUNCH_VERSION = '1.0';
